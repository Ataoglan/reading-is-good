spring:
  datasource:
    url: jdbc:mysql://localhost:3380/readingisgood?reWriteBatchedInserts=true
    user-name: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL57Dialect

